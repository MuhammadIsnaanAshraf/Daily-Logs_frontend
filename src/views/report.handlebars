<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        h1 {
            font-size: 26px;
            color: #fff;
            text-transform: uppercase;
            font-weight: 300;
            text-align: center;
            margin-bottom: 15px;
            padding-top: 10px;
        }
        h1 p {
            margin-top: 15px;
            font-size: 18px;
        }

        table {
            width: 100%;
            table-layout: fixed;
        }

        .tbl-header {
            background-color: rgba(255, 255, 255, 0.3);
        }

        .tbl-content {
            margin-top: 0px;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        th {
            padding: 20px 15px;
            text-align: left;
            font-weight: 600;
            font-size: 12px;
            color: #fff;
            text-transform: uppercase;
        }

        td {
            padding: 15px;
            text-align: left;
            vertical-align: top;
            font-weight: 300;
            font-size: 12px;
            color: #fff;
            line-height: 18px;
            border-right: solid 2px rgba(255, 255, 255, 0.1);
            border-bottom: solid 1px rgba(255, 255, 255, 0.1);
        }

        td span {
            font-weight: bold;
        }

        /* demo styles */
        body {
            background: -webkit-linear-gradient(left, #25c481, #25b7c4);
            background: linear-gradient(to right, #25c481, #25b7c4);
            font-family: 'Poppins', sans-serif;
        }

        section {
            margin: 20px;
        }

        tr {
            font-family: 'Poppins', sans-serif;
        }

        /* follow me template */
        .made-with-love {
            margin-top: 40px;
            padding: 10px;
            clear: left;
            text-align: center;
            font-size: 10px;
            font-family: 'Poppins', sans-serif;
            color: #fff;
        }

        .made-with-love i {
            font-style: normal;
            color: #F50057;
            font-size: 14px;
            position: relative;
            top: 2px;
        }

        .made-with-love a {
            color: #fff;
            text-decoration: none;
        }

        .made-with-love a:hover {
            text-decoration: underline;
        }


        /* for custom scrollbar for webkit browser*/

        ::-webkit-scrollbar {
            width: 6px;
        }

        ::-webkit-scrollbar-track {
            -webkit-box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.3);
        }

        ::-webkit-scrollbar-thumb {
            -webkit-box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.3);
        }
    </style>
</head>

<body>
    <section>
        <!--for demo wrap-->
        <h1>Daily Task Report
            <br>
             <p>Hi {{lookup hr 'name'}} this is daily tasks report of your team members</p>
        </h1>
       
        <div class="tbl-header">
            <table cellpadding="0" cellspacing="0" border="0">
                <thead>
                    <tr>
                        {{!-- {{#each columnKeys as |columnKey|}}
                            <th>{{columnKey}}</th>
                        {{/each}} --}}
                        <th>Sr#</th>
                         <th>Name</th>
                          <th>Email</th>
                           <th>Role</th>
                            <th>Tasks</th>
                    </tr>
                </thead>
            </table>
        </div>
        <div class="tbl-content">
            <table cellpadding="0" cellspacing="0" border="0">
                <tbody>
                     {{#each users as |user|}}
                        <tr>
                            <td>{{inc @index }}</td>
                            <td>{{lookup user.user 'name'}}</td>
                            <td>{{lookup user.user 'email'}}</td>
                            <td>{{lookup user.user 'role'}}</td>

                            <td>
                            {{#if user.userLogs}}
                                {{#each user.userLogs.logs as |log|}}
                                <span> Title :</span> {{log.title}}
                                <br>
                                <br>
                                <span> TimeTaken :</span> {{log.timeTaken}}
                                <br>
                                <br>
                                <span> Description :</span> {{log.description}}
                                {{/each}}
                            {{else}}
                            no tasks{{/if}} {{!-- This is a comment --}}
                            </td>
                        </tr>
                     {{/each}}

                </tbody>
            </table>
        </div>
    </section>


</body>
<script>
    console.log(report)
</script>
</html>